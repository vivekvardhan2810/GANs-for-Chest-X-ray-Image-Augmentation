# GANs-for-Chest-X-ray-Image-Augmentation
This project implements a Generative Adversarial Network (GAN) to generate synthetic chest X-ray images and improve the performance of a pneumonia classification model.  Medical datasets are often small and imbalanced, which leads to poor generalization. This project addresses that problem using GAN-based data augmentation.
